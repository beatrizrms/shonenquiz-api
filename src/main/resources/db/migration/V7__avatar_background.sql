-- ============================================================
--  V7 — Avatar: cenário de fundo (renomeia coluna cosplay → background)
-- ============================================================

ALTER TABLE cat_avatars RENAME COLUMN cosplay TO background;
